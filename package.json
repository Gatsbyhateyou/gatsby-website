{
  "name": "gatsby-website",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
  "dev": "vite",
  "build": "node scripts/generate-supabase-config.js && node scripts/generate-analytics-config.js && vite build",
  "preview": "vite preview",
    "deploy": "git add -A && git commit -m \"deploy\" && git push"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
