{
  "name": "gatsby-website",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
  "dev": "vite",
  "build": "node scripts/copy-styles.js && node scripts/generate-supabase-config.js && node scripts/generate-analytics-config.js && vite build",
  "preview": "vite preview",
    "deploy": "git add -A && git commit -m \"deploy\" && git push",
    "screenshot": "node scripts/screenshot-preview.js","next-style":"node scripts/next-style-to-optimize.js"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "playwright": "^1.49.0",
    "vite": "^7.3.1"
  }
}
