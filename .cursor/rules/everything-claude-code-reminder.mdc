---
description: 大型修改时提醒使用 Everything Claude Code (ECC)，并说明如何用 ECC 做当前操作
alwaysApply: true
---

# Everything Claude Code (ECC) 使用提醒

## 何时触发

当用户提出**大型修改**类需求时，若用户**没有**通过 ECC 工作流（如 `/plan`、`/tdd`、`/code-review`）来发起，**必须先提醒**再执行。

**「大型修改」包括但不限于：**

- 新增重要功能（如新页面、新模块、登录/支付、多步流程）
- 大范围重构、架构调整、多文件联动修改
- 涉及安全/数据/配置的改动（认证、存储、环境变量）
- 用户明确说「大改」「重构」「做个完整功能」「多页/多模块」等

**不视为大型修改（可不提醒）：**

- 单文件小改动、文案/样式微调、修一个小 bug、单条日记新增

## 提醒方式

1. **简短提醒**：说明本项目已安装 Everything Claude Code，做大型修改时建议先用 ECC 工作流，质量更稳、有规划有审查。
2. **针对当前操作给出用法**：根据用户具体需求，说明「这一步」该怎么用 ECC，例如：
   - **要做新功能** → 建议先用 `/plan "功能简述"` 或说「先做实现计划再写代码」，再按计划实现；实现后可用 `/code-review` 审查。
   - **要写/补测试** → 建议用 `/tdd` 或说「按 TDD 做 xxx」。
   - **要改很多文件/重构** → 建议先 `/plan "重构目标"`，再分步改；改完用 `/code-review` 或 `/refactor-clean`。
   - **修构建/部署问题** → 可用 `/build-fix`；若涉及安全配置，可提一句 `/security-scan`。
3. **让用户选择**：问用户是「先按 ECC 走一遍（推荐）」还是「直接按你的描述改」。若用户选直接改，则按用户要求执行，不再重复提醒本条。

## 一句话总结

**大型修改未用 ECC 时 → 先提醒 + 说明「针对你提的这一步」该怎么用 ECC（用哪条命令/怎么说），再问是否先走 ECC 还是直接做。**
